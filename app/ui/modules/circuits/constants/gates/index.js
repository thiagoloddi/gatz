import * as and from './lib/and.constants';
import * as _switch from './lib/switch.constants';

export default  {
  and, switch: _switch
};

export const GATE = "GATE";
export const AND = 'and';
export const SWITCH = 'switch';